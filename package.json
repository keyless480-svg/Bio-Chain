{
  "name": "biochain-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && cp -r frontend/dist dist || xcopy /E /I frontend\\dist dist"
  }
}
